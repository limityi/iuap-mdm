<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yonyou.iuap.project.repository.SettlementmethodRepository">

    <resultMap id="settlementmethodMap" type="com.yonyou.iuap.project.entity.Settlementmethod">
        <id property="id" column="mdm_pk" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="selectOnlyValidateData" resultMap="settlementmethodMap">
        SELECT * FROM UAP65.MDM_SETTLEMENTMETHOD WHERE code IN (SELECT code FROM UAP65.MDM_SETTLEMENTMETHOD GROUP BY code HAVING COUNT (*) > 1) ORDER BY code
    </select>

    <select id="selectRequiredData" resultMap="settlementmethodMap" parameterType="java.util.List">
        select
        *
        from
        UAP65.MDM_SETTLEMENTMETHOD
        where
        <foreach collection="list" index="index" item="item" separator="or" open="(" close=")">
            ${item} is null
        </foreach>
    </select>

    <select id="selectAllData" resultMap="settlementmethodMap" parameterType="java.util.Map">
        SELECT
        *
        FROM
        UAP65.MDM_SETTLEMENTMETHOD
        <if test="searchParam !=null and searchParam !='null'.toString()">
            WHERE
            code like '%${searchParam}%'
            or
            name like '%${searchParam}%'
        </if>
    </select>
</mapper>
